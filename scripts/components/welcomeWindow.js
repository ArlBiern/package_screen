const welcomeWindow = () => {
  let htmlContent = `
    <h4 class="info_heading">Możesz mieć swoją przesyłkę w kilkanaście sekund.</h4>
    <p class="info_paragraph">Rozpocznij klikając w przycisk.</p>
    <button class="button welcomeButton">Rozpocznij</button>
    `;

  return htmlContent;
};

export default welcomeWindow;
